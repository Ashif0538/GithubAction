name: linting-python-code

on:
  push:
    branches:
      - main

jobs:
  python-linting-code:
    runs-on: ubuntu-latest
    steps:
      - name: Super-Linter
        uses: super-linter/super-linter@v6.3.0
